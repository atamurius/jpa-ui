<input class="form-control input-sm"
       name="{{property.name}}"
       value="{{&value}}"
       type="{{type}}"
       {{{attributes}}}
       id="field_{{property.name}}"
       placeholder="{{property.displayName}}" />
